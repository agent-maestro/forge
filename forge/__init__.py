"""Top-level Forge package.

Currently only exposes the `forge.blocks` standard library of
pre-verified EML computation blocks. As Forge grows new
public-facing utilities (loop runners, pipeline schedulers, etc.)
they slot in here.
"""

from __future__ import annotations
