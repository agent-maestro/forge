// arrhenius.eml -- chemical reaction rate. Chain order 1 (exp).

module arrhenius;

const R: f64 = 8.314;  // ideal gas constant J/(mol K)

fn rate(A: f64, Ea: f64, T: f64) -> f64
  where chain_order <= 1,
        domain: T > 0.0
{
    A * exp(-Ea / (R * T))
}
