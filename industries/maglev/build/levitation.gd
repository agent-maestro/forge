# Generated by EML-lang GDScript backend.
# Source module: levitation
# Source file:   industries/maglev/levitation.eml
# Functions:     3
# Constants:     11
#
# Attach this script to a Node, or register it as an
# Autoload singleton (Project Settings > Autoload) to
# call its kernels module-style: Levitation.kernel(args).

extends Node
class_name Levitation

const ZERO: float = 0.0
const G: float = 9.80665
const I_MIN: float = 0.1
const I_MAX: float = 3.0
const Z_MIN: float = 0.001
const Z_MAX: float = 0.05
const M_MIN: float = 0.001
const M_MAX: float = 2.0
const K_MIN: float = 1e-06
const K_MAX: float = 0.001
const LIFT_MARGIN: float = 1.1

# magnetic_force
# Chain order: 0    Cost class: p0-d2-w0-c0    EML depth: 2
static func magnetic_force(k: float, current: float, gap: float) -> float:
	return (((k * current) * current) / (gap * gap))

# lift_margin
# Chain order: 0    Cost class: p0-d3-w0-c0    EML depth: 3
static func lift_margin(k: float, mass: float) -> float:
	var f_floor = (((k * I_MIN) * I_MIN) / (Z_MAX * Z_MAX))
	var f_required = ((LIFT_MARGIN * mass) * G)
	return (f_floor - f_required)

# net_force
# Chain order: 0    Cost class: p0-d3-w0-c0    EML depth: 3
static func net_force(k: float, current: float, gap: float, mass: float) -> float:
	return ((((k * current) * current) / (gap * gap)) - (mass * G))
