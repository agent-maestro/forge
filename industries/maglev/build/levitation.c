/*
 * Generated by EML-lang C backend
 * Source module: levitation
 * Source file:   industries\maglev\levitation.eml
 * Functions:     3
 * Constants:     11
 * Types:         0
 */

#include "libmonogate.h"
#include <stdint.h>
#include <math.h>

static const double ZERO = 0.0;
static const double G = 9.80665;
static const double I_MIN = 0.1;
static const double I_MAX = 3.0;
static const double Z_MIN = 0.001;
static const double Z_MAX = 0.05;
static const double M_MIN = 0.001;
static const double M_MAX = 2.0;
static const double K_MIN = 1e-06;
static const double K_MAX = 0.001;
static const double LIFT_MARGIN = 1.1;

/*
 * magnetic_force
 * Chain order: 0     Cost class: p0-d2-w0-c0
 * EML depth:   2  Drift risk: LOW
 * Dynamics:    0 osc, 0 decay  (predicted_r=0)
 * FPGA est:   2 MAC, 0 exp, 0 ln, 0 trig -> 4 cy @ 32-bit
 */
double magnetic_force(double k, double current, double gap) {
    return (((k * current) * current) / (gap * gap));
}

/*
 * lift_margin
 * Chain order: 0     Cost class: p0-d3-w0-c0
 * EML depth:   3  Drift risk: LOW
 * Dynamics:    0 osc, 0 decay  (predicted_r=0)
 * FPGA est:   3 MAC, 0 exp, 0 ln, 0 trig -> 6 cy @ 32-bit
 */
double lift_margin(double k, double mass) {
    double f_floor = (((k * I_MIN) * I_MIN) / (Z_MAX * Z_MAX));
    double f_required = ((LIFT_MARGIN * mass) * G);
    return (f_floor - f_required);
}

/*
 * net_force
 * Chain order: 0     Cost class: p0-d3-w0-c0
 * EML depth:   3  Drift risk: LOW
 * Dynamics:    0 osc, 0 decay  (predicted_r=0)
 * FPGA est:   3 MAC, 0 exp, 0 ln, 0 trig -> 6 cy @ 32-bit
 */
double net_force(double k, double current, double gap, double mass) {
    return ((((k * current) * current) / (gap * gap)) - (mass * G));
}
