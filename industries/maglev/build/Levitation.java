// Generated by EML-lang Java backend
// Source module: levitation
// Source file:   industries\maglev\levitation.eml
// Functions:     3
// Constants:     11

public final class Levitation {

    private Levitation() {}  // utility class -- no instances

    public static final double ZERO = 0.0;
    public static final double G = 9.80665;
    public static final double I_MIN = 0.1;
    public static final double I_MAX = 3.0;
    public static final double Z_MIN = 0.001;
    public static final double Z_MAX = 0.05;
    public static final double M_MIN = 0.001;
    public static final double M_MAX = 2.0;
    public static final double K_MIN = 1e-06;
    public static final double K_MAX = 0.001;
    public static final double LIFT_MARGIN = 1.1;

    /**
     * magnetic_force
     * <p>Pfaffian profile: chain_order=0, cost_class=p0-d2-w0-c0, drift_risk=LOW.</p>
     *
     * @param k (type Real)
     * @param current (type Real)
     * @param gap (type Real)
     * @return Real
     * @forge.requires (k >= K_MIN)
     * @forge.requires (k <= K_MAX)
     * @forge.requires (current >= ZERO)
     * @forge.requires (current <= I_MAX)
     * @forge.requires (gap >= Z_MIN)
     * @forge.requires (gap <= Z_MAX)
     * @forge.ensures (result >= ZERO)
     * @forge.verify lean theorem=magnetic_lift_force_nonnegative
     */
    public static double magnetic_force(double k, double current, double gap) {
        return (((k * current) * current) / (gap * gap));
    }

    /**
     * lift_margin
     * <p>Pfaffian profile: chain_order=0, cost_class=p0-d3-w0-c0, drift_risk=LOW.</p>
     *
     * @param k (type Real)
     * @param mass (type Real)
     * @return Real
     * @forge.requires (k >= K_MIN)
     * @forge.requires (k <= K_MAX)
     * @forge.requires (mass >= M_MIN)
     * @forge.requires (mass <= M_MAX)
     * @forge.verify lean theorem=levitation_envelope_lifts_with_margin
     */
    public static double lift_margin(double k, double mass) {
        final double f_floor = (((k * I_MIN) * I_MIN) / (Z_MAX * Z_MAX));
        final double f_required = ((LIFT_MARGIN * mass) * G);
        return (f_floor - f_required);
    }

    /**
     * net_force
     * <p>Pfaffian profile: chain_order=0, cost_class=p0-d3-w0-c0, drift_risk=LOW.</p>
     *
     * @param k (type Real)
     * @param current (type Real)
     * @param gap (type Real)
     * @param mass (type Real)
     * @return Real
     * @forge.requires (k >= K_MIN)
     * @forge.requires (k <= K_MAX)
     * @forge.requires (current >= ZERO)
     * @forge.requires (current <= I_MAX)
     * @forge.requires (gap >= Z_MIN)
     * @forge.requires (gap <= Z_MAX)
     * @forge.requires (mass >= M_MIN)
     * @forge.requires (mass <= M_MAX)
     */
    public static double net_force(double k, double current, double gap, double mass) {
        return ((((k * current) * current) / (gap * gap)) - (mass * G));
    }

}
